/**
 * Core Logging
 *
 * Translated from quran-ios/Core/VLogging
 * Provides structured logging with configurable handlers.
 */

export {
  LogLevel,
  type LogMetadata,
  type LogHandler,
  ConsoleLogHandler,
  logger,
  createLogger,
} from './logger';
